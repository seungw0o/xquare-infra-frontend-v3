export const typographySemantic = {
    xquare: {
        fontFamily: '"Pretendard Variable", "Pretendard", -apple-system, BlinkMacSystemFont, system-ui, "Segoe UI", sans-serif',
    }
} as const;

export type TypographySemantic = typeof typographySemantic;